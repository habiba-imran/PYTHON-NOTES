import shutil
# shutil.copy("source", "dest") # dest file is created if doesnot exist
# shutil.copy2("source", "data") # keeps the metadata
# shutil.copytree("source", "data") # copies folders
# shutil.move("source", "dest") # equivalant to rename
# shutil.rmtree("source") # deletes a folder
# shutil.rm("source") # deletes a file
# explore other functions yourself


